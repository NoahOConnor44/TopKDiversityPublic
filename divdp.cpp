#include <iostream>
#include "divdp.hpp"
#include "helperFunctions.hpp"
#include "divastar.hpp"
#include "node.hpp"

divdp::divdp()
{

}

// need to get connected components from the original graph passed in
vector<node> divdp::doDivDp(vector<node> *graph, int k)
{
    /*
    helperFunctions help;
    divastar astar;
    vector<node> result;
    vector<vector <node>> connectedComponents; 
    for(int i = 0; i < connectedComponents.size(); i++)
    {
        //vector<node> temp = connectedComponents.at(i);
        //vector<node> result2 = astar.doDivAStar(&temp,k);
        //result = help.plusOperator(result,result2);
    }
    return result;
    */
}